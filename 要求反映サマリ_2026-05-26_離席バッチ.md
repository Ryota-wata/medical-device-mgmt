# 要求一覧0526 反映サマリ（2026-05-26 離席中バッチ）

> 正本: `docs/customer-requests/要求一覧0526.xlsx`（Ph1 182要求）
> 方針: REQ原文の対象×操作のみ忠実反映（推測実装なし）。反映可否/方法が不明・新規画面・大規模構造変更は**保留**（ユーザー承認方針）。
> コミット: 画面/要求単位（push なし）。`git log 9f619e9..HEAD` で追跡可。
> 検証: 各 tsc(型エラー0) 通過。到達可能な画面は Playwright で目視確認（下表「検証」欄）。

---

## 1. 対応した要求（今回実装・コミット済み）

| REQ | 対象画面（パス） | 対応内容 | 検証 |
|---|---|---|---|
| REQ-047 | 見積登録STEP（`app/quotation-data-box/{rfq-process,category-registration,item-ai-matching}` + `components/StepProgressBar.tsx`） | STEP③「登録区分登録」→「明細区分登録」、STEP④「個体品目AI判定」→「資産マスタ登録」を全出現箇所で改称 | Playwright（item-ai-matching でHeader/STEP3/STEP4表示確認） |
| REQ-081 | 修理STEP②（`app/repair-task/page.tsx`） | 「見積を登録」表記を「見積書の登録」に統一（ボタンは既存、alert/案内文を改称） | tsc・grep |
| REQ-132 | 貸出可能機器閲覧（`app/lending-available/page.tsx`） | 「貸出種別名」→「貸出グループ名」 | Playwright（見出し確認） |
| REQ-119 | 申請モーダル（`components/ui/{Purchase,Update,Addition}ApplicationModal.tsx`） | 優先順位を1〜10選択＋「その他(11位以上)」フリー入力化（購入申請：共通として3モーダルに反映） | tsc |
| REQ-065 | 資産登録（`app/quotation-data-box/asset-registration/page.tsx`） | 「会計区分」カラム追加（17区分のドロップダウン） | Playwright（17値確認） |
| REQ-066 | 資産登録（同上） | 「勘定科目」カラム追加（14科目のドロップダウン）。旧「仮勘定科目」読取専用列を置換 | Playwright（14値確認） |
| REQ-018 | 現有資産調査①（`app/survey-location/page.tsx`） | 入力項目を 調査日・カテゴリ・棟・階・部門・部署 に再構成（棟→階連動、部門/部署は個別部署マスタ、室名は調査②へ移し当画面から除去） | Playwright（項目確認） |
| REQ-043 | 新規購入申請 内容確認モーダル（`app/quotation-data-box/components/ApplicationDetailModal.tsx`） | 依頼情報節（依頼No/申請部署/申請者）追加、申請品目節を拡充（品目名/数量/優先順位/設置部署/設置室名/希望納期）、フッターにキャンセル追加（却下は申請中のみ維持） | Playwright（AP-2025-0002で全節確認） |
| REQ-052 | STEP② OCR明細確認（`app/quotation-data-box/components/QuotationRegistrationModal/Step2OcrResultDisplay.tsx`） | 明細行に「操作」列＋行削除ボタン、品目名/型式をインライン編集化（別行に分かれた品目/型式を統合→不要行削除） | tsc（UIはOCRモーダル深部のためコード検証） |
| REQ-121 | 更新・増設申請モーダル（`components/ui/{Update,Addition}ApplicationModal.tsx`） | 「現在の接続状況/接続先」を確認表示・入力・リセット・stateから除去（4/30決定「申請モーダルで表示しない」）。システム接続要望は維持 | tsc・grep（完全除去確認） |
| REQ-058 | STEP⑥ 登録確認（`app/quotation-data-box/registration-confirm/page.tsx`） | 見積フェーズ「発注登録用見積」時のみ「見積情報Databaseに登録」隣に「発注登録」ボタンを条件表示（登録→発注登録起点へ継続） | tsc（条件表示。既定mockはphase'定価'で非表示） |
| REQ-076 | 修理タスク管理リスト（`app/quotation-data-box/components/RepairRequestsTab.tsx`） | 修理区分=院内対応の行は見積依頼/見積登録/発注登録を経ず「作業日登録」から操作 | Playwright（院内対応行で作業日登録、外部依頼は従来） |

## 2. 既に実装済みだった要求（検証のみ・コード変更なし）

| REQ | 対象画面 | 状況 |
|---|---|---|
| REQ-082 | 修理STEP②（`app/repair-task/page.tsx`） | 業者名/担当者/見積金額(税別)/会計区分/「見積書の登録」＋発注用時の決済No/発注書プレビュー/発注登録(→印刷・mail送信モーダル)が既に実装済み。要求仕様を充足（コード確認） |

## 3. 今セッション以前に反映済み（参考・xlsx列未更新）

REQ-019/020/021/023（現有資産調査②）、REQ-025/026/027/028/029（現調修正画面）、REQ-032/033/034（台帳取込AI）は本セッション以前に実装・検証済み（`画面改修サマリ_2026-05-26.md` 参照）。xlsxの「モック反映」列は未更新。

---

## 4. 保留した要求（理由付き）

### A. 新規画面・新規ロール・新規モーダル/タブの追加（=新規構築）
| REQ | 対象 | 保留理由 |
|---|---|---|
| REQ-049 / REQ-050 | STEP① SHIP登録依頼用UI | SHIP依頼分を一括登録する専用ロール＋画面の新規構築。要確認事項も含む |
| REQ-064 | 検収（モバイル） | 業者検収専用権限でのログイン＋発注グループ選択遷移という新規権限/導線 |
| REQ-146 | リモデル編集リスト | タスク管理にリモデル専用「廃棄・移動」管理タブの新規追加 |
| REQ-149 / REQ-150 | 編集カード（リモデル） | 見積グループ作成・申請種別ボタン群、購入申請用モーダルの新規構築 |
| REQ-162 / REQ-163 | 編集カード（通常） | 同上（見積グループ作成・申請種別反映、購入申請用モーダル）の新規構築 |
| REQ-165 | リモデルタスク管理 | 通常タスク管理同様の一式化＋絞込（リモデルタスク管理全体の大規模化） |

### B. データモデル拡張が必要（推測実装回避）
| REQ | 対象 | 保留理由 |
|---|---|---|
| REQ-044 / REQ-045 | 更新/増設 申請内容確認モーダル | 共有モーダルの共通節（依頼情報/申請品目）はREQ-043で統一済み。ただし「更新/増設対象機器」節の シリアルNo・購入年月日・更新対象機器の確認情報 が `PurchaseApplicationAsset` 型に無く、忠実反映には型＋モックデータ拡張が必要 |
| REQ-067 / REQ-068 / REQ-075 | 移動/廃棄/修理 申請内容確認モーダル | 品目情報の シリアルNo・購入年月日 等がデータモデルに無く、また専用フッターアクション（承認して原本に反映／院内対応・外部依頼 等）の整備を要する。型・データ拡張を伴う中規模対応のため保留 |

### C. 中〜大規模のUI構造改修
| REQ | 対象 | 保留理由 |
|---|---|---|
| REQ-005 | 共通：全モーダル | モーダルの拡大/縮小/移動は全モーダル横断の大規模機能。優先度も「MUSTに近いWONT」 |
| REQ-060 | 検収（`asset-provisional-registration` 1222行） | 品目別の納品日入力欄追加＋一括登録/個別上書きUI＋題名変更（納品・検収日登録）の一体改修。中規模構造改修のため保留 |
| REQ-168 / REQ-169 / REQ-172 | 原本リスト（`app/asset-search-result/page.tsx` 782行） | 絞込みヘッダー(設置場所/品目/管理部署/全体検索)・絞込み/並べ替え・原本修正追記モード(編集リスト相当の編集機能)。特にREQ-172は編集モード新設で大規模 |
| REQ-113 | 点検 設定変更 | 点検機器登録用モーダルを設定変更モードで開く導線の新設。既存モーダルの編集対応＋導線整備で中規模、要仕様確認 |

### D. 仕様が曖昧／実質WONT（要顧客確認）
| REQ | 対象 | 保留理由 |
|---|---|---|
| REQ-016 | 個別部署マスタ | 原文が記号的（旧/新/ID/共通M/諸室区分①②）で確定仕様が読み取れない。要顧客確認 |
| REQ-115 | 点検 全般 | 点検中のカルテ参照。背景に「ブラウザを2つ開ければ問題無し」とあり実質WONT/任意 |

---

## 5. 「要確認（△）」27件の扱い

xlsxで「△（モックでは判断できない）」の27要求は、反映可否・方法の判断に顧客確認が必要なため本バッチでは着手せず**保留（QA候補）**として扱う。`docs/coverage/req-impl-gap/coverage-matrix.md` の該当行に△として記録済み。

---

## 6. 次アクション（ローカルレビュー時の確認観点）

1. 本サマリ表「対応した要求」を画面で確認（dev: `npm run dev`）。OCR明細(REQ-052)・発注登録ボタン(REQ-058)は条件/深部のため操作手順に留意。
2. 保留 B群（044/045/067/068/075）は、対象機器のデータ項目（シリアルNo/購入年月日 等）を型に追加してよいか方針決定 → 決定後に実装可能。
3. 保留 C群（060/168/169/172/113）は中〜大規模のため、優先順位と仕様を確認のうえ着手判断。
4. 保留 A群（新規画面/ロール/タブ）と D群（016/115）は顧客QA。
