# 医療機器管理システム - 画面モック

Next.js App Router + Zustand で構築する業務システムの画面モックプロジェクト。

## モック実装ルール

画面を実装・修正する際は、以下の制約を常に守ること。
詳細なレビューが必要な場合は各Skillを明示的に呼び出す（例: `app/order/page.tsx をUI Skillsでレビューして`）。

---

## 1〜4. UI / レイアウト / デザイン品質 / UXフロー

画面を実装・修正するときは、対象に応じて次のSkillに従う。**判断に迷う実装（新規画面の作成、レイアウトの作り直し、配色やタイポグラフィの決定、登録フローの設計）を行う場合は、着手前に該当Skillを読む。**

| 対象 | Skill | 主な内容 |
|---|---|---|
| コンポーネント・インタラクション・タイポグラフィ・パフォーマンス | `.claude/skills/ui-skills/` | `aria-label`、`AlertDialog`、`h-dvh`、`tabular-nums`、要素の使い分け（button/radio/checkbox）、`useEffect` の乱用回避 |
| 入力幅・グリッド・スペーシング・密度・テーブル | `.claude/skills/layout-skills/` | 期待データ長に合わせた入力幅、`1fr` と `max-width`、8pxグリッド、1行3項目まで、金額の右寄せ |
| 色・タイポグラフィ・シャドウ・角丸・視覚的階層 | `.claude/skills/design-quality-skills/` | トークン化、コントラスト比、純黒禁止、フォントスケール、ボタン階層 |
| アクション配置・段階的開示・完了後の誘導・確認パターン・状態遷移 | `.claude/skills/ux-flow-skills/` | Primary CTA は1つ、条件付き項目のトリガー表示、完了画面の次アクション、空/アクティブ/完了の3状態 |

レビューを依頼された場合も同様（例: `app/order/page.tsx をUI Skillsでレビューして`）。

---

## 5. データソース

### 資産検索
- プルダウン・曖昧検索での資産検索は必ず **マスタデータ（Zustand Store）** から取得する
- コンポーネント内に独自の `MOCK_ASSETS_*` 定数を定義しない
- 検索対象データは `useMasterStore()` の `assets` または `facilities` を使用
- 検索フィルターのオプション生成もマスタデータから動的に取得する

---

## 6. フィルター機能（全画面共通ルール）

フィルター機能を実装する際は、以下のルールを必ず遵守すること。

### コンポーネント
- **プルダウンは `SearchableSelect` コンポーネントを使用**（曖昧検索対応）
- 通常の `<select>` は使用禁止
- オプションデータはマスタデータ（`useMasterStore()`）から取得

### 階層フィルターの連動ルール

#### 1. 子フィルター選択時に親フィルターを自動設定
- 品目を選択 → 中分類・大分類・categoryを自動設定
- 中分類を選択 → 大分類・categoryを自動設定
- 大分類を選択 → categoryを自動設定
- 部門を選択 → 部署を自動設定

#### 2. 親フィルター変更時に子フィルターをクリア
- categoryを変更 → 大分類・中分類・品目を検証し、無効な場合はクリア
- 大分類を変更 → 中分類・品目を検証し、無効な場合はクリア
- 中分類を変更 → 品目を検証し、無効な場合はクリア
- 部署を変更 → 部門を検証し、無効な場合はクリア

#### 3. 子フィルターのオプションは親に連動
- 大分類オプション → 選択されたcategoryに基づいて絞り込み
- 中分類オプション → 選択されたcategory・大分類に基づいて絞り込み
- 品目オプション → 選択されたcategory・大分類・中分類に基づいて絞り込み
- 部門オプション → 選択された部署に基づいて絞り込み

### 階層構造
```
部署系: department → section
資産系: category → largeClass → mediumClass → item
```

### 実装リファレンス
- フック: `lib/hooks/useAssetFilter.ts`
- コンポーネント: `components/ui/SearchableSelect.tsx`

---

## 7. 成果物作成基準（`docs/deliverable-standards.md`）

納品物（設計書・テスト仕様書・ドキュメント等）を生成・更新する際は、必ず `docs/deliverable-standards.md` の基準に従うこと。

### 対象納品物（7点）
1. 画面設計書（Excel）— 全63画面のレイアウト・項目定義・操作仕様
2. 画面遷移設計書（Excel）— 全画面間の遷移定義・ロール別アクセス
3. DB設計書（Word + PlantUML/SVG）— 94テーブル+ビューの定義・ER図
4. API設計書（Word）— エンドポイント・リクエスト/レスポンス・認証認可
5. ソフトウェア一式（ZIP）— 全63画面実装・ビルド成功・型エラー0
6. テスト仕様書及びエビデンス（Excel）— テスト項目・実施結果・エビデンス
7. 運用ドキュメント（Word/PDF）— 障害復旧手順書・VPN導入手順書

### 必須チェック
- 各成果物の「必須構成」と「品質基準」をすべて満たすこと
- 成果物間の整合性（画面↔DB↔API↔テスト）を維持すること
- 検収基準書（`docs/検収基準書.xlsx`）の48項目が判定根拠

### 画面設計書 専用ルール

開発はオフショアチームのため翻訳ステップが必要。**画面設計書を作成・更新するときは、必ず `.claude/skills/screen-design-doc-generator/` のワークフロー（Step 1〜8）に従う。** ルール・手順の詳細はスキルファイルを参照。

特に次の2点は省略不可。

- **R0 正本の切り分け**: 画面項目・文言は画面モックが正。データ型・パラメータ・パス・enum は API設計書が正。mock 内部の識別子（Reactコンポーネント名・store型・state名）は設計書に書かない
- **R6 ナンバリング必須**: 要素表を直して Excel を再生成しただけでは未完了。`data-element-id` 付与 → anchors 定義 → 番号バッジ付きスクショの生成まで行って初めて完了

その他の成果物のスキル: 画面遷移設計書は `.claude/skills/screen-transition-diagram/`、総合テスト仕様書は `.claude/skills/test-spec-generator/`。

---

## 8. Figmaデザイン反映（`.claude/skills/figma-reflection/`）

Figma画像から既存モックを更新する際は、必ずスキルのワークフロー（Step 1〜9）に従う。
ルール・手順の詳細はスキルファイルを参照。

### 画像の命名規則
- **ファイル名 = `app/` 直下のルートフォルダ名**（例: `login.png`, `survey-location.png`）
- 1画面1画像。セクション画像（複数画面を含む画像）は使用禁止
- `design/input/` に配置 → `node design/distribute.mjs` で自動配置

### 完了判定の必須ゲート（`.claude/skills/figma-reflection-gate/`）

Figma反映関連の作業で「完了」「終わった」「コミットした」「やり直し完了」をユーザーへ報告する**直前に必ず `figma-reflection-gate` スキルを 1 画面ごとに 1 回通る**。

- 9-Step（棚卸し / Figma 取得 / アイコン / トークン / 差分判定 / 実装 / ブラウザ検証 / ビルド / 報告）の履行確認
- 1 つでも未達があれば「完了」と報告してはならない
- スキップ可能な条件は **存在しない**（ユーザーが明示的に許可した場合を除く）
- 報告には **画面名（日本語 + パス）** を必ず含める。コード番号（B-5 等）だけの報告は禁止
- 過去の違反履歴（2026-05-12/13 セッション）はスキル内に永久記録

---

## 9. タスク実行ルール

### 絶対条件（最優先・例外なし）: 全タスクは目的に向かう
- **すべてのタスク・作業は、合意された目的（ゴール）に直結していなければならない。** 目的に資さないタスクには着手しない。
- **複数タスクの並行・追加タスクの開始によって目的から逸れることは絶対にあってはならない。** 新しいタスク・寄り道・思いつきが発生したら、着手前に必ず「これは現在の目的に直結するか」を判定し、逸れる/不明なら着手せずユーザーに確認する。
- **進行中の目的を中断して別目的の作業へ流れない。** 順序・優先はゴールから逆算して決める。中断が必要なら明示的にユーザーへ確認する。
- 実例（再発防止）: 2026-05-26、要求反映（Step①）が20%で停止したまま Figma 反映や随時依頼へ逸れていた。「ギャップ識別＝反映完了」と取り違え、目的（全要求の mock 反映）から逸脱していた。二度と繰り返さない。

### 指示の解釈
- **「把握しろ」「理解しろ」** → ユーザーが提供した情報を整理して確認するだけ。ファイル調査は不要
- **「改修しろ」「実装しろ」** → このタイミングでファイルを読み、実装に着手する
- ユーザーが既に要件を説明している場合、それを繰り返し調査しない

### 長時間タスクの回避
- 1つの指示で複数ステップが必要な場合、**まずタスクをブレイクダウンして提案**する
- 各ステップは独立して完了可能な単位に分割する
- ユーザーの承認を得てから順番に着手する

### Git操作
- `git push` はユーザーの明示的な許可があるまで実行しない

### 禁止事項
- 単純な指示に対して過剰な調査を行わない
- ユーザーが提供した情報を無視してファイルを読み始めない
- 存在しないファイルを探し続けない（1回の失敗で止める）
- **指示された範囲を超えて実装しない**（「フィルターを実装しろ」→フィルターのみ。テーブルは含まない）
- 先回りして追加実装しない。追加が必要なら確認を取る

---

## 10. プラグイン使用ルール

ゴール（mockを正本に①画面設計書②総合テスト仕様書を自動生成）に資するプラグインを使用する。

### playwright（必須・中核）
- **用途**: 画面モックのブラウザ検証。**「要求がmockに正しく反映されたか」の合否判定**（✓判定のDoDの一部）。
- **使用タイミング**:
  - 画面モック実装・修正後の動作確認（クリック・フォーム入力・画面遷移）
  - 各Ph1要求の反映検証
  - 顧客レビュー前の品質チェック
- **要求×検証の記録（2026-05-26）**: 各Ph1要求の検証は「要求ID × 検証手順 × 期待結果 × 結果」をカバレッジ・マトリクスに記録し、**総合テスト仕様書のテストケース原型として再利用**する。検証＝成果物の入力。

### context7
- **用途**: ライブラリ・フレームワーク・SDK・API・CLIの最新ドキュメント取得
- **使用タイミング**:
  - Next.js / Zustand / Tailwind / React 等のAPI仕様確認
  - ライブラリのバージョン移行・設定方法
  - 「知っているつもり」でも、training dataが古い可能性があるため必ず確認
- **注意**: リファクタリング・ビジネスロジックデバッグ・一般的なプログラミング概念には使わない

### code-reviewer（必須から除外・任意 / 2026-05-26）
- **必須ループから除外**。理由: (1) 本プロジェクトのゴールはmockを正本とする画面設計書・総合テスト仕様書の生成であり、担保すべきは「要求→mock反映の忠実性」。捨てメディアであるmockのコード品質レビューはレバレッジが低い。(2) API キー未設定で実質機能していなかった。
- 要求反映の品質は **playwright検証＋カバレッジ・マトリクスのDoD（実装＋検証＋要求原文一致）** で担保する。
- 大規模・高リスク変更で必要と判断した場合のみ、手動で任意実施する。

### 運用ルール
- 使用する際は、使用したことをユーザーに報告する
- playwright / context7 を使わない合理的理由がある場合のみ、その理由を明示してスキップする
- **要求反映の検証（playwright）は省略しない**（✓判定のDoD）

---

## 11. 成果物タスクの共通プロセス（必須・例外なし）

成果物（画面設計書・画面遷移設計書・テスト仕様書・運用ドキュメント・mock改修 等）を伴うタスクは、**次の1〜5を1プロセスとして完了まで回す**。**3で止めてユーザーへ報告するのは違反**。

1. **作成/実装** — 正本（API設計書・要求・顧客レイアウトイメージ・mock）に基づいて作成する。
2. **厳格レビューを受ける** — `deliverable-reviewer` で独立レビューを実施（自己判断で省略しない）。レビュー依頼には「目的・対象ファイル・正本の所在・重点観点」を明示する。
3. **指摘を反映** — Blocker / Major は原則すべて是正。見送る場合は理由を明示して記録する。
4. **再レビューして合格させる** — 修正後に**再度レビューへかける**。**合格（Blocker / Major ゼロ）になるまで 2〜4 を繰り返す**。1回のレビューで終わらせない。
5. **合格後にユーザーへ提示** — 合格して初めて、成果物と「レビュー観点・指摘・対応結果（見送り理由含む）」をユーザーへ報告する。

### 合格判定の3原則（2026-07-15 追加。違反実績あり）

**① 合格基準は「提出物そのもの」で判定する**
- ソース（elements MD）だけを見て合格にしない。**実際の提出物（Excel）を開き、仕様書として読めるかを確認**して初めて合格。
- 具体: 要素表に要素以外の行（API突合表・差異表・凡例・`----` 区切り・重複ヘッダー）が混入していないか、列と値が対応しているか、番号とスクショのバッジが一致しているかを実物で確認する。
- 違反実績: MD基準で合格にした結果、Excelの要素表に API①〜⑳・D-1〜D-5 が列ズレで混入したまま提出し、ユーザーが一目で発見した。

**② レビュー依頼で結論を誘導しない**
- 依頼文に「**〜は別タスクとして切り出す前提**」「**昇格不要なら許容と明示してください**」「本画面単体では解決不能」等の、**結論・スコープ判断を先出しする記述を書かない**。事実と対象だけを渡し、判定はレビュワーに委ねる。
- 違反実績: 上記①の欠陥はレビュワーが2度検出（Major→中）していたが、依頼文で「解決不能・切り出す前提・昇格不要なら許容と明示」と誘導した結果、格下げされ合格した。前提の「解決不能」自体が誤りだった（生成器の修正は短時間で完了）。

**③ 「横断課題だから」は、成果物に見える欠陥を通す理由にならない**
- 共通スクリプト起因・他画面にも波及、という理由で**提出物に見えている欠陥を合格させない**。直せるなら直す（共通スクリプトの修正を含む）。
- どうしても当該タスクで直せない場合のみ、**欠陥が残っている事実を明示**した上で合格可否を判断する。「読み手が誤解しない」等の推測で通さない。

### 補足
- レビュワー: 成果物＝`deliverable-reviewer` / mockのUI・UX＝`uiux-reviewer`（必要時）。
- 「レビュー指摘を直したので完了」は**再レビュー未実施＝未完了**。合格の確認まで含めて1タスク。
- 共通スクリプト（生成器・撮影スクリプト等）を修正した場合は、**全画面への影響を機械的に検証**してから完了とする（要素数の増減比較等）。
- 合格後、Codexが出力をレビューする場合がある。

---

## 12. プロジェクトマネジメント（`.claude/skills/pm-guide/`）

プロジェクト運営の判断が必要な場面では `pm-guide` スキルに従う。全社PM研修資料（`docs/PMナレッジ/`）を判断に使える形へ再構成したもの。

### 対象となる場面
- 計画立案（スコープ／スケジュール／コスト／体制／品質／リスク）
- 進捗・品質のモニタリング指標の設定と、報告の定量化
- **各工程の完了基準（Done定義）の判断**
- **問題発生時の原因診断と打ち手の検討**（炎上パターン・阻害要因マトリクス）
- 工程ごとの実務手順（要件定義・設計・実装/UT・テスト・移行）

### 正本の優先順位（競合時は必ずこの順で解決）

1. ユーザー（PM本人）の明示的な判断
2. **本ファイル（`CLAUDE.md`）**
3. `docs/deliverable-standards.md`（検収基準書ベース）
4. `pm-guide` スキル（全社PM標準）

とくに `docs/PMナレッジ/工程別成果物一覧(V1.00).xlsx`（全社標準）と `docs/deliverable-standards.md`（本PJの検収基準）は守備範囲が重なる。**本PJの納品物については後者が正**であり、全社標準を根拠に本PJの成果物基準を変更してはならない。

### PM系スキルの構成

| スキル | 役割 |
|---|---|
| `pm-guide` | **判断の土台**。工程の完了基準、要素別の計画・モニタリング、問題発生時の原因診断 |
| `pm-risk-register` | **標準リスク管理表**（全社標準xlsx）でのリスク起票・スコア判定・対策方針決定 |
| `pm-wbs-evm` | **標準WBS（簡易EVM版）**でのWBS作成・進捗の定量判定（PV/EV/AC・SPI/CPI） |

リスク・WBS/進捗の依頼は、`pm-guide` ではなく**該当する生成系スキルを通す**（判定ロジックがテンプレート側に実装されており、しきい値は原則変更不可のため）。

### 各成果物スキルとの関係
`pm-guide` は**工程をどう回すかの指針**であり、成果物の作り方そのものは各スキルが優先する（画面設計書＝`screen-design-doc-generator`、画面遷移設計書＝`screen-transition-diagram`、テスト仕様書・テスト方式＝`test-spec-generator`）。
